#!/usr/bin/env bash
# Configuration pour synchroniser la base locale vers la base distante (Infomaniak)
# NE PAS committer de mots de passe en clair. Ce fichier est lu par scripts/sync-db-to-remote.sh
# Vous pouvez exporter les variables dans votre shell à la place.

# --- Base locale ---
# Hôte et port de la base locale (ex: 127.0.0.1 et 3306)
LOCAL_DB_HOST="127.0.0.1"
LOCAL_DB_PORT="3306"
# Nom de la base locale
LOCAL_DB_NAME="fusepoint_db"
# Identifiants locale (évitez de committer des secrets; mettez-les via export dans votre shell)
LOCAL_DB_USER="fusepoint_db"
LOCAL_DB_PASS="FusepointBD2025!"

# --- Base distante (Infomaniak) ---
# Hôte MySQL Infomaniak (ex: tt3ae.myd.infomaniak.com ou 57-104359.mysql.db)
REMOTE_DB_HOST="tt3ae.myd.infomaniak.com"
REMOTE_DB_PORT="3306"
REMOTE_DB_NAME="tt3ae_fusepoint"
REMOTE_DB_USER="tt3ae_sam"
REMOTE_DB_PASS="JbW4D~7.@91.aGs"

# --- SSH vers le serveur distant ---
# Identité SSH (utilisez une clé si possible). Exemple tiré de scripts/deploy.config
REMOTE_USER="ZDaULDMYSEC_sam"
REMOTE_HOST="57-104359.ssh.hosting-ik.com"
REMOTE_PATH="/srv/customer/sites/fusepoint.ch"

# Options dump/import
# Inclure routines/événements/triggers si vous en utilisez
INCLUDE_ROUTINES="true"
INCLUDE_EVENTS="true"
INCLUDE_TRIGGERS="true"

# Compression des dumps
COMPRESS="true"

# Mettre à true pour un essai sans import (backup et transfert uniquement)
DRY_RUN="false"